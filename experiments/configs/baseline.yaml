name: "Baseline RAG Evaluation"
description: "Simple retrieval-generation baseline with BM25 and GPT-4o-mini"
run_id: "v0.0.0-baseline"
pipeline_type: "baseline"

retrieval_config:
  strategy: "bm25"
  top_k: 5
  k1: 1.5
  b: 0.75

llm_configs:
  generator:
    provider: "openai"
    model: "gpt-4o-mini"
    temperature: 0.0
    max_tokens: 256
    api_key_env_var: "OPENAI_API_KEY"

hyperparameters: {}
  # Optional: subset for testing
  # subset_size: 100
